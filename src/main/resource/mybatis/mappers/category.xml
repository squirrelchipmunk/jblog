<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="category">
	
	<insert id="insertCategory" parameterType="CategoryVo">
		<![CDATA[
			insert into category
			values( seq_category_no.nextval,
					#{id},
					#{cateName},
					#{description},
					sysdate
			)
		]]>
	</insert>
	
	<select id="selectCateList" parameterType="String" resultType="CategoryVo">
		<![CDATA[
			select cateNo,
				   cateName,
				   regDate
			from category
			where id= #{id}
			order by regDate desc
		]]>
	</select>

	
	<select id="selectAllDataList" parameterType="String" resultType="CategoryVo">
		<![CDATA[
			select cateNo,
				   cateName,
				   regDate,
				   description
			from category
			where id= #{id}
			order by regDate desc
		]]>
	</select>
	
	<select id="selectPostNum" parameterType="CategoryVo" resultType="int">
		<![CDATA[
			select count(postNo)
			from category c, post p
			where c.cateNo = p.cateNo and
				  c.cateNo = #{cateNo} and
				  id = #{id}
		]]>
	</select>
	
	<delete id="selectCategory" parameterType="int">
		<![CDATA[
			delete from category
			where cateNo = #{cateNo}
		]]>
	</delete>
	
</mapper>